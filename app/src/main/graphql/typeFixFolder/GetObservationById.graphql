query GetObservationById($id:token) {
    Observation(_id:$id){
        id
        status
        code {
            coding {
                system
                version
                code
                display
                userSelected
            }
            text
        }
        valueString
        valueInteger
        valueDateTime
    }
}