query GetPersonById($id:token) {
    Person(_id:$id){
        name {
            ...name
        }
        birthDate
        gender
        active
        address {
            ...address
        }
        extension {
            url
            valueBoolean
            valueDecimal
        }
    }
}